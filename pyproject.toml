[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sales-agent"
version = "0.1.0"
description = "AI sales outreach agent with Inkbox and OpenAI"
requires-python = ">=3.11"
dependencies = [
    "inkbox",
    "openai",
]
